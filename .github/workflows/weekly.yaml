name: Weekly

# Verifies the supported Flink range, which the per-PR CI deliberately does not:
# verify.yaml builds one version for fast feedback (the same split Flink's own
# externalized connectors use between push_pr.yml and weekly.yml).
#
# Six things are checked here:
#   * every supported minor still compiles and passes the suite
#   * a jar built against the floor still runs on the newest supported minor
#     without recompiling, which is what lets this project publish one artifact
#     for the whole range instead of one per minor
#   * the 1.x LTS lane still builds and passes the whole suite from this same
#     source (the lts row — see the version policy in CLAUDE.md and #32)
#   * the next unreleased Flink gets an early warning, so upstream changes are
#     found before they are released rather than after
#   * a newly released Flink minor is noticed at all — see new_minor_check
#   * the pinned licence-text sources still serve the checked-in bytes even
#     when nothing in the repository changed — verify.yaml re-fetches them
#     only on a licence-input change — see notice_sources
on:
  schedule:
    # Sundays. Off the hour, because everything scheduled at :00 lands on the
    # runner queue at the same instant.
    - cron: '37 18 * * 0'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: read

# The two versions this workflow names. Everything else is derived: the floor is
# whatever the pom pins, and verify.yaml names no version at all.
#
# They live here rather than in the matrix because GitHub does not expose the
# env context to `strategy`, and repeating a version across matrix rows is how
# one of them gets missed. So matrix rows carry a *role* — which end of the
# supported range they are — and the version is resolved in a step.
env:
  # The newest supported Flink minor. Bumped when Flink releases a new minor,
  # together with flink.version in the pom; new_minor_check fails until it is.
  FLINK_CEILING: '2.3.0'
  # Upstream early warning: the next Flink that does not exist yet.
  FLINK_NEXT_SNAPSHOT: '2.4-SNAPSHOT'
  # The 1.x LTS, built from this same source (#32): the lts row compiles and
  # tests the whole suite against it, selecting the flink1 compat source root
  # (the one-interface seam described at the flink.compat property in pom.xml).
  # Bumping to a new 1.20 patch is an edit here — dependabot does not see
  # workflow env, the same accepted staleness FLINK_CEILING has.
  FLINK_LTS: '1.20.4'

jobs:
  # Compile and test against each end of the supported range, plus the next
  # unreleased Flink.
  compile_and_test:
    # Scheduled workflows are inherited by forks; this keeps them from running
    # there. The immutable ID of flink-gcp/flink-connector-gcp, not the owner
    # name: a fork gets its own ID, while a transfer or a rename changes the
    # name and would switch a name guard off silently, leaving every job in
    # this file green and idle. The three guards below are the same test.
    if: github.repository_id == '1305440656'
    name: ${{ matrix.role }} (JDK ${{ matrix.jdk }})
    runs-on: ubuntu-latest
    timeout-minutes: 40
    strategy:
      # One version failing must not hide the state of the others — that is the
      # whole point of running a matrix.
      fail-fast: false
      matrix:
        include:
          # The floor is not named anywhere in this workflow: its row passes no
          # -Dflink.version, so what gets tested is whatever the pom pins.
          #
          # On JDK 21 rather than 17, because floor-on-17 is what verify.yaml runs on
          # every push and what binary_compat rebuilds below — floor-on-21 is the
          # corner of the matrix nothing else reaches.
          - role: floor
            jdk: '21'
          - role: ceiling
            jdk: '17'
          # Flink 2.x declares Java 11/17/21, but flink-connector-parent compiles
          # to target 17, so 11 cannot run this connector at all. 17 and 21 are
          # spread across the range rather than run as a full cross product:
          # doubling both axes multiplies emulator-backed integration-test
          # minutes for little added signal.
          - role: ceiling
            jdk: '21'
          # Not marked continue-on-error on purpose: a break here is the signal
          # this row exists to deliver, and a green-looking failed job would
          # bury it.
          - role: next
            jdk: '17'
          # The 1.x LTS, from this same source (#32): compiled per version via
          # the flink1 compat root, so this is source-level support — no
          # cross-major binary claim, which is why binary_compat below does not
          # involve it. JDK 17 only: it is the one JDK both supported majors
          # share with verify.yaml.
          - role: lts
            jdk: '17'
    steps:
      # Every job pins the same commit. Without this a merge landing mid-run
      # makes one version look like it silently skipped tests — that false
      # positive has already happened once on this repository.
      #
      # Every row checks out main: the 1.x LTS is built from this same source
      # (the lts row), not from a version branch — #32 measured the whole API
      # delta down to one compile-only interface, so the kafka-style
      # branch-per-line model this comment used to anticipate was not needed.
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ github.sha }}

      - uses: actions/setup-java@b6effb05e454b25005698d916606bdc6ffcbf961 # v5.7.0
        with:
          distribution: temurin
          java-version: ${{ matrix.jdk }}
          cache: maven

      # Unpinned, and install-action rather than mise: see the note on the same
      # step in verify.yaml.
      - uses: taiki-e/install-action@1ed6d7be6168f6c9046541087ff549b6bc581fdf # v2.87.2
        with:
          tool: just

      - name: Resolve the Flink version for this row
        id: resolve
        run: |
          case '${{ matrix.role }}' in
              floor)   args='' ;;
              ceiling) args="-Dflink.version=$FLINK_CEILING" ;;
              next)    args="-Dflink.version=$FLINK_NEXT_SNAPSHOT -Pflink-snapshots" ;;
              lts)     args="-Dflink.version=$FLINK_LTS -Dflink.compat=flink1" ;;
              *)       echo "::error::unknown role '${{ matrix.role }}'"; exit 1 ;;
          esac
          echo "args=$args" >> "$GITHUB_OUTPUT"
          # Recorded so the log says which versions a green run actually attests
          # to, including for the floor row, which names none.
          echo "pom floor  : $(./mvnw -q -DforceStdout help:evaluate -Dexpression=flink.version)"
          echo "maven args : ${args:-(none)}"

      # The floor row passes no arguments at all, so what it tests is whatever
      # the pom pins — that is the point, and `just verify` takes the rest
      # through to Maven unchanged.
      #
      # -Dtest.excluded.groups=gated narrows the root pom's `gated,slow` back to
      # `gated`, which is what makes this the lane the `slow` classes run in.
      # Dropping it would fail nothing here: those classes would simply stop
      # running anywhere and every workflow would stay green, so
      # `just check-gated-tags` fails on this file if the opt-in disappears while
      # a `slow` tag still exists.
      - name: Build and verify
        run: >-
          just verify -Pdocs-snippets -Dtest.excluded.groups=gated
          ${{ steps.resolve.outputs.args }}

  # Does one artifact cover the whole supported range?
  #
  # Flink guarantees source compatibility across minors for @Public and nothing
  # for @PublicEvolving or @Experimental, and only about half the Flink surface
  # this connector touches is @Public — so "one jar runs on every supported
  # minor" is a measurement, not something the annotations promise. This job is
  # that measurement: build once against the floor, then re-run the suite with
  # the ceiling swapped onto the classpath and nothing recompiled.
  #
  # Floor and ceiling are the only two supported minors, so there is one pair to
  # check and no matrix. Widening the policy past two would bring one back.
  binary_compat:
    if: github.repository_id == '1305440656'
    name: Binary compat (floor-built, run on the ceiling)
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ github.sha }}

      - uses: actions/setup-java@b6effb05e454b25005698d916606bdc6ffcbf961 # v5.7.0
        with:
          distribution: temurin
          java-version: '17'
          cache: maven

      # Unpinned, and install-action rather than mise: see the note on the same
      # step in verify.yaml.
      - uses: taiki-e/install-action@1ed6d7be6168f6c9046541087ff549b6bc581fdf # v2.87.2
        with:
          tool: just

      # One step rather than four, because the four are a sequence whose order
      # is load-bearing — the fingerprint has to be captured between the two
      # runs, and the surefire execution ids decide whether the ITCases run at
      # all — and it is a sequence somebody reruns by hand the moment this job
      # goes red. It has one definition, in the justfile, which is where those
      # constraints are written down. The cost is that a failure here says which
      # `==>` phase failed rather than which step.
      - name: Build on the floor, re-run on Flink ${{ env.FLINK_CEILING }}
        run: just binary-compat "$FLINK_CEILING"

  # Nothing else would tell us a new Flink minor shipped.
  #
  # The supported range is "the current and the previous minor", so a Flink
  # release silently makes this repository's claim wrong — and the dependabot PR
  # that used to be the de-facto notification is now suppressed on purpose (see
  # the ignore rule in dependabot.yml), because triaging it monthly was the
  # problem. That trade only works if something else raises its hand.
  #
  # Deliberately independent of the jobs above rather than a dependency of them:
  # a new upstream release must not stop the current range from being verified.
  new_minor_check:
    if: github.repository_id == '1305440656'
    name: New Flink minor released?
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ github.sha }}

      # Unpinned, and install-action rather than mise: see the note on the same
      # step in verify.yaml.
      - uses: taiki-e/install-action@1ed6d7be6168f6c9046541087ff549b6bc581fdf # v2.87.2
        with:
          tool: just

      # The ceiling is passed explicitly. Run by hand the recipe derives it from
      # this file, so either way FLINK_CEILING above stays its only home.
      - name: Compare the ceiling against Maven Central
        run: just check-flink-release "$FLINK_CEILING"

  # The offline NOTICE check never consults the recorded licence-text sources,
  # and verify.yaml re-fetches them only when a change touches a licence-source
  # input — so an upstream that moves under an unchanged repository (a deleted
  # tag, an edited archived head) is caught here or nowhere (issue #343). The
  # header of scripts/check-notice-sources.sh carries the failure modes.
  #
  # Independent of the Flink-range jobs above on purpose: a broken upstream
  # ref must not stop the range from being verified, nor the other way round.
  notice_sources:
    if: github.repository_id == '1305440656'
    name: Licence sources still serve the pinned texts?
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ github.sha }}

      - uses: actions/setup-java@b6effb05e454b25005698d916606bdc6ffcbf961 # v5.7.0
        with:
          distribution: temurin
          java-version: '17'
          cache: maven

      # Unpinned, and install-action rather than mise: see the note on the same
      # step in verify.yaml.
      - uses: taiki-e/install-action@1ed6d7be6168f6c9046541087ff549b6bc581fdf # v2.87.2
        with:
          tool: just

      # check-notice.py needs tomllib (Python >= 3.11) — installed deliberately
      # rather than trusted from the runner image, as in verify.yaml.
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: '3.12'

      - name: Regenerate the NOTICEs from the pinned sources and diff
        run: just check-notice-sources

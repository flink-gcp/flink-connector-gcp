name: Lint

# Linters for the parts of the repository Maven does not cover. Spotless and
# checkstyle handle the Java sources inside `just verify` (ci.yaml); this is
# where everything else goes — shellcheck, ruff, actionlint,
# `tofu fmt -check` (`tofu validate` is subsumed by the plan the tofu-plan
# workflow runs on exactly the pull requests that touch opentofu/), and the
# Flink API tier audit (issue #103), which fails on a new main-source
# dependency on an @Internal, @Experimental or unannotated Flink type.
#
# Split out of ci.yaml so it reports in seconds instead of behind the
# integration tests. That is the whole reason: keeping mise away from
# setup-java was also claimed, but the shims only lead the JDK because
# add_shims_to_path defaults to true, which a job that needs both can simply
# turn off.
on:
  push:
    branches:
      - main
      - v1.20
    paths: &lint_paths
      - 'scripts/**'
      # actionlint lints every workflow, so every workflow is an input here —
      # not just this one.
      - '.github/workflows/**'
      # The justfile is deliberately absent: `just lint` runs no linter over it
      # (see the note on the recipe), so it is the thing that invokes a lint
      # here, not an input to one.
      # A shellcheck or actionlint version bump must run them. mise.toml is
      # where those versions are declared, so leaving it out would skip the lint
      # in exactly the change that most needs it.
      - 'mise.toml'
      # tofu fmt -check reads these.
      - 'opentofu/**'
      # The API tier audit reads the org.apache.flink imports in every main
      # source root (java, java-flink1, java-flink2) — a new import is exactly
      # what it exists to catch —
      - '*/src/main/java*/**'
      # and classifies them at the pom-pinned flink.version.
      - 'pom.xml'
  pull_request:
    paths: *lint_paths
  workflow_dispatch:

# NOTE: the paths filter means pull requests that touch none of the above never
# report this workflow at all. That is fine while it is optional, but a required
# check that never reports blocks a pull request forever — so making this one
# required means either dropping the filter or adding a job that reports success
# when the filter does not match. Same exposure as docs.yaml.

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

permissions:
  contents: read

jobs:
  lint:
    name: Lint scripts and workflows
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      # install_args limits this to the linters — mise.toml pins the tools
      # every recipe needs, and this job wants none of the others. Their
      # versions come from mise.toml, so they are declared once and are the same
      # locally and here, and not the runner image's copy: its shellcheck is
      # 0.9.0 on ubuntu-24.04 and 0.11.0 on 26.04, so an ubuntu-latest migration
      # would fail a pull request that changed nothing. The action caches the
      # install by default, keyed partly on install_args.
      - uses: jdx/mise-action@9e7f7633ff6f6d6048a9418a68d48f288f50eb14 # v4.2.3
        with:
          install_args: shellcheck actionlint ruff opentofu

      # just comes from install-action even though mise is right here, so that
      # every workflow installs it the same way. See the note in ci.yaml.
      - uses: taiki-e/install-action@3d7d7cd5ac7f994c1892ae0c06165095b9139094 # v2.85.1
        with:
          tool: just

      - name: Run the linters
        run: just lint

      # check-flink-api-tiers.py needs python >= 3.11 (tomllib). Same shape as
      # ci.yaml's check-notice step: setup-python rather than mise, keeping the
      # install_args above limited to the linters. No cache option on purpose:
      # the script has zero pip dependencies, so there is nothing to cache, and
      # `cache: pip` without a requirements file fails the step.
      - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6.3.0
        with:
          python-version: '3.12'

      - name: Audit the Flink API tiers the main sources depend on
        run: just check-flink-api-tiers
